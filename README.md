# Floorplan Generator Stage

Procedural floorplan generation and Stage simulation launcher. Generates office-style environments from TOML configurations and launches them in Stage.

**Clone using:** `git clone --recurse-submodules git@github.com:npragin/floorplan-generator-stage.git`